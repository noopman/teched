using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace GitDemo.Tests
{
	[TestClass]
	public class TheTests
	{
		[TestMethod]
		public void SomeTest()
		{
			Assert.Fail("This test is supposed to fail to show that the Git option currently does not run tests!");
		}
	}
}